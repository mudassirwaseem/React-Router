import React from 'react'

function Home() {
    return (
        <div style={{ width: "90%", margin: "auto" }}>
            <h1 style={{fontWeight:"bold",textAlign:"center"}} >React Router DOM</h1>
            <h2>Quick Start</h2>
            <p>To get started with React Router in a web app, you’ll need a React web app. If you need to create one, we recommend you try Create React App. It’s a popular tool that works really well with React Router.</p>
            <h2>Installation</h2>
            <p>You can install React Router from the public npm registry with either npm or yarn. Since we’re building a web app, we’ll use react-router-dom in this guide.npm install react-router-dom
Next, copy/paste either of the following examples into src/App.js.</p>

<hr></hr>
<p> this example we have 3 “pages” handled by the router: a home page, an about page, and a users page. As you click around on the different</p>
       
<h2>1st Step</h2>
            <p>To get started with React Router in a web app, you’ll need a React web app. If you need to create one, we recommend you try Create React App. It’s a popular tool that works really well with React Router.</p>
            <h2>2nd Step</h2>
            <p>You can install React Router from the public npm registry with either npm or yarn. Since we’re building a web app, we’ll use react-router-dom in this guide.npm install react-router-dom
Next, copy/paste either of the following examples into src/App.js.</p>

<hr></hr>
<p> this example we have 3 “pages” handled by the router: a home page, an about page, and a users page. As you click around on the different</p>
        
<h2>3rd Step</h2>
            <p>To get started with React Router in a web app, you’ll need a React web app. If you need to create one, we recommend you try Create React App. It’s a popular tool that works really well with React Router.</p>
            <h2>4th Step </h2>
            <p>You can install React Router from the public npm registry with either npm or yarn. Since we’re building a web app, we’ll use react-router-dom in this guide.npm install react-router-dom
Next, copy/paste either of the following examples into src/App.js.</p>

<hr></hr>
<p> this example we have 3 “pages” handled by the router: a home page, an about page, and a users page. As you click around on the different</p>
        
        </div>
    )
}

export default Home
